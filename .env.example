# Convergence Platform Configuration
# Copy this file to .env and update with your values

# ============================================================================
# Network Device Credentials
# ============================================================================

# Default credentials for network devices
NETWORK_USERNAME=admin
NETWORK_PASSWORD=your_network_password
NETWORK_ENABLE_PASSWORD=your_enable_password

# SNMP Configuration
SNMP_COMMUNITY=public

# Nautobot Configuration for Device Enrichment
# Get API token from Nautobot UI: Admin → Users → API Tokens
# API docs available at: https://YOUR_NAUTOBOT_IP/api/docs/
NAUTOBOT_URL=https://192.168.1.36
NAUTOBOT_TOKEN=your-api-token-here
NAUTOBOT_VERIFY_SSL=false  # Set to false for self-signed certs in development
SNMP_V3_USERNAME=snmpv3user
SNMP_V3_PASSWORD=snmpv3password
SNMP_V3_PRIV_PASSWORD=snmpv3privpassword

# ============================================================================
# Grafana Configuration
# ============================================================================

GRAFANA_ADMIN_USERNAME=admin
GRAFANA_ADMIN_PASSWORD=admin

# ============================================================================
# VictoriaMetrics Configuration
# ============================================================================

# Retention period for metrics (e.g., 30d, 90d, 1y)
VM_RETENTION_PERIOD=90d

# ============================================================================
# OpenTelemetry Collector Configuration
# ============================================================================

# Log level (debug, info, warn, error)
OTEL_LOG_LEVEL=info

# Batch processing
OTEL_BATCH_SIZE=1024
OTEL_BATCH_TIMEOUT=10s

# ============================================================================
# Platform Configuration
# ============================================================================

# Environment (development, staging, production)
ENVIRONMENT=development

# Timezone
TZ=UTC

# ============================================================================
# Optional: API Keys for External Services
# ============================================================================

# PagerDuty integration key (for alerting)
# PAGERDUTY_INTEGRATION_KEY=your_key_here

# Slack webhook URL (for notifications)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ============================================================================
# Alerting Configuration (Phase 3)
# ============================================================================

# Webhook URL for Alertmanager/Grafana alert notifications
# Can be a Slack Incoming Webhook, custom HTTP endpoint, or automation trigger
# ALERT_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Discord webhook URL — used by the Grafana Discord contact point and automation agent notifications
# Server Settings → Integrations → Webhooks → New Webhook → Copy URL
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN

# Discord Bot — enables /approve, /reject, /pending slash commands in your Discord server
# Without this, approval still works via REST API but requires curl/HTTP client
# Setup: https://discord.com/developers/applications → New Application → Bot → Reset Token
# OAuth2 scopes: bot + applications.commands | Permissions: Send Messages, Use Slash Commands
# DISCORD_BOT_TOKEN=your_bot_token_here
# Your server (guild) ID for instant slash command sync (vs ~1h global propagation delay)
# Developer Mode: User Settings → Advanced, then right-click server icon → Copy Server ID
# DISCORD_GUILD_ID=your_guild_id_here

# Email address for critical alert notifications
# ALERT_EMAIL=admin@example.com

# Alertmanager external URL (used in alert annotations)
ALERTMANAGER_URL=http://localhost:9093

# Grafana alert evaluation interval
GRAFANA_ALERT_EVAL_INTERVAL=1m

# ============================================================================
# Phase 4: Threat Intelligence API Keys
# ============================================================================

# AbuseIPDB — https://www.abuseipdb.com/register (free tier: 1000 checks/day)
ABUSEIPDB_API_KEY=your_abuseipdb_api_key_here

# GreyNoise — https://www.greynoise.io (community API works without key)
GREYNOISE_API_KEY=your_greynoise_api_key_here

# AlienVault OTX — https://otx.alienvault.com/accounts/signup
OTX_API_KEY=your_otx_api_key_here

# IPInfo — https://ipinfo.io/signup (free tier: 50k lookups/month)
IPINFO_TOKEN=your_ipinfo_token_here

# Anthropic (Claude Haiku for AI narratives) — https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# LLM provider — "anthropic" (default) or "ollama"
# Set to "ollama" to use a local/remote Ollama instance instead of the Anthropic API.
# LLM_PROVIDER=anthropic

# Ollama — used when LLM_PROVIDER=ollama
# Point at the Ollama instance on your host machine (or any reachable endpoint).
# On Linux, host.docker.internal resolves to the Docker host via extra_hosts mapping.
# OLLAMA_BASE_URL=http://host.docker.internal:11434
# OLLAMA_MODEL=llama3.2:3b

# ============================================================================
# Phase 5: Automation Agent
# ============================================================================

# pfSense — connection (shared by all paths)
# PFSENSE_HOST=192.168.1.1
# PFSENSE_VERIFY_SSL=false       # false = accept self-signed certs (typical for home/SOHO)

# pfSense — Path A: REST API v2 (pfSense Plus 25.11, optional)
# Preferred when available. Manages a firewall alias via the official REST API.
# Setup:
#   1. System > API > Keys — generate a key, assign it to an admin user
#   2. Firewall > Aliases — create a Host alias named AutoAgent_Block_v4
#   3. Firewall > Rules — add a block rule with Source = AutoAgent_Block_v4,
#      placed above your pass rules
#
# PFSENSE_API_KEY=your_pfsense_api_key_here
# PFSENSE_FIREWALL_ALIAS=AutoAgent_Block_v4

# pfSense — Path B: XML-RPC exec_php (no API key needed — just web UI credentials)
# Appends to a pfBlockerNG custom list file and triggers a sync.
# Persistent across reboots and pfBlockerNG reloads.
# Setup:
#   pfBlockerNG > IP > IPv4 Custom Lists — add list "pfBlockerNG_AutoAgent_v4"
#
# PFSENSE_XMLRPC_USER=admin
# PFSENSE_XMLRPC_PASS=your_pfsense_web_password

# pfSense — Path C: SSH emergency fallback (optional)
# Runtime-only: pfctl table add does not survive a reboot.
# Setup:
#   ssh-keygen -t ed25519 -f pfsense_id_ed25519 -C "convergence-autoagent" -N ""
#   # Add public key: System > User Manager > admin > Authorized SSH Keys
#   # Mount private key via docker-compose.yml volumes section
#
# PFSENSE_SSH_HOST=192.168.1.1   # defaults to PFSENSE_HOST if blank
# PFSENSE_SSH_USER=admin
# PFSENSE_SSH_KEY_PATH=/app/secrets/pfsense_id_ed25519

# ---------------------------------------------------------------------------
# Safety controls (defaults are intentionally conservative)
# ---------------------------------------------------------------------------
# Master kill-switch: true = log & Discord-notify only, never touch pfSense
DRY_RUN=true
# composite_score must be >= this to even consider a block action
AUTO_ACTION_THRESHOLD=80
# composite_score must be >= this to skip Discord approval and auto-execute
# Set to 101 to always require human approval for every action
AUTO_APPROVE_THRESHOLD=95
# Hard cap: never execute more than N live actions per rolling 60-minute window
MAX_ACTIONS_PER_HOUR=5
# Temporary block duration (hours)
BLOCK_TTL_HOURS=24
# How often the agent polls threat-intel for new high-risk IPs (seconds)
POLL_INTERVAL_SECONDS=600

# ============================================================================
# Development Settings
# ============================================================================

# Enable debug mode
DEBUG=false

# Enable hot reload for configs
HOT_RELOAD=true

# Mock telemetry generator
GENERATE_MOCK_DATA=false
